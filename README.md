# 1. This repo contains multiple assets transfer tools.
For example: 

asset_to_habitat: generate scene configuration files for habitat simualtor from a folder of glb meshes.

glb2usd: convert glb meshes to usd meshes with texture.

usd2glb: convert usd meshes to glb files with texture. 